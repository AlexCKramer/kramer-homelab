global:
  scrape_interval: 15s # How often to ping the nodes

scrape_configs:
  - job_name: 'proxmox_nodes'
    static_configs:
      - targets: 
        - '192.168.42.5:9100' # MOTHERSHIP
        - '192.168.42.8:9100' # HOLODECK